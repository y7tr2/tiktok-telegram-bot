services:
  - type: web
    name: tiktok-telegram-bot
    env: node
    rootDir: bot
    buildCommand: npm install && npx tsc && pip3 install -U yt-dlp
    startCommand: node dist/index.js
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: RENDER_URL
        sync: false
